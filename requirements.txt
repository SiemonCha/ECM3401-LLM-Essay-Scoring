# requirements.txt
# ECM3401: Measuring Semantic Robustness in LLM-Based Essay Scoring
# Python 3.10+ (tested on 3.10.x and 3.11.x)
# Installation: pip install -r requirements.txt

# ============================================================================
# Core Data Science
# ============================================================================
pandas==2.0.3          # DataFrame operations, CSV handling
numpy==1.24.4          # Numerical computations
scipy==1.11.4          # Statistical tests (t-tests, correlations)

# ============================================================================
# Machine Learning
# ============================================================================
scikit-learn==1.3.2    # Confusion matrices, Cohen's Kappa, metrics

# ============================================================================
# PyTorch (MPS Support for Apple Silicon)
# Stable version with excellent MPS support for M1/M2/M3 Macs
# ============================================================================
torch==2.1.2           # Deep learning framework
torchvision==0.16.2    # Image operations (not heavily used)
torchaudio==2.1.2      # Audio operations (not used)

# ============================================================================
# LLM APIs
# For GPT-4o-mini inference via OpenAI API
# ============================================================================
openai==2.11.0         # OpenAI API client
httpx==0.28.1          # Async HTTP client (OpenAI dependency)
httpcore==1.0.9        # HTTP core library (OpenAI dependency)

# ============================================================================
# Transformers Ecosystem
# For Phi-3-Mini local inference on Apple Silicon
# ============================================================================
transformers==4.44.2   # HuggingFace transformers (Phi-3 support)
accelerate==0.34.2     # Multi-GPU/MPS acceleration
sentence-transformers==2.7.0  # Sentence embeddings (for potential few-shot)
tokenizers==0.19.1     # Fast tokenization
safetensors==0.4.5     # Safe model serialization format

# ============================================================================
# HuggingFace Hub
# For downloading Phi-3-Mini model (~8GB)
# ============================================================================
huggingface-hub==0.25.2  # Model downloads from HuggingFace

# ============================================================================
# Vector Search (Optional)
# For potential few-shot retrieval experiments (not required for basic experiments)
# ============================================================================
faiss-cpu==1.8.0.post1   # Vector similarity search

# ============================================================================
# OpenAI Utilities
# For token counting and API cost estimation
# ============================================================================
tiktoken==0.7.0        # OpenAI's token counter (BPE tokenization)

# ============================================================================
# Text Processing
# For essay analysis, readability metrics, and feature extraction
# ============================================================================
nltk==3.8.1            # Natural Language Toolkit
textstat==0.7.3        # Readability metrics (Flesch-Kincaid, etc.)
spacy==3.7.4           # NLP pipeline for TTR + lexical density analysis
                       # After install: pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
stanza==1.9.2          # Stanford NLP - constituency parsing for parse tree depth
                       # After install: python -c "import stanza; stanza.download('en')"

# ============================================================================
# Visualization
# For generating publication-quality figures
# ============================================================================
matplotlib==3.8.4      # Plotting library
seaborn==0.13.2        # Statistical visualizations (built on matplotlib)

# ============================================================================
# Utilities
# Progress bars, environment variables, config files, HTTP requests
# ============================================================================
tqdm==4.66.5           # Progress bars for loops
python-dotenv==1.0.1   # Load environment variables from .env file
pyyaml==6.0.2          # YAML configuration file parsing
requests==2.32.3       # HTTP library for API requests

# ============================================================================
# Data Validation
# For configuration validation and type checking
# ============================================================================
pydantic==2.9.2        # Data validation using Python type annotations

# ============================================================================
# Jupyter (Optional - for exploration notebooks)
# Uncomment these lines if you want to use Jupyter notebooks
# ============================================================================
# jupyter==1.0.0
# jupyterlab==4.0.13
# ipykernel==6.29.5

# ============================================================================
# INSTALLATION NOTES
# ============================================================================
# Tested Environment:
#   - Ubuntu 24.x with AMD Radeon RX 7900 XT (ROCm)
#   - Python 3.10+
#
# Installation Commands:
#   pip install -r requirements.txt --break-system-packages
#   pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
#   python -c "import stanza; stanza.download('en')"   # ~500MB, one-time
#
# For ROCm (AMD GPU) PyTorch, replace torch lines above with:
#   pip install torch --index-url https://download.pytorch.org/whl/rocm6.0
#
# API Keys Required:
#   Create .env file in project root with:
#     OPENAI_API_KEY=sk-proj-your-key-here
#     HUGGINGFACE_TOKEN=hf_your-token-here  # Optional for Phi-3 download